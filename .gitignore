# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out
*.pprof

# Dependency directories (remove the comment below to include it)
# vendor/

# vscode workspace/user settings
.vscode/settings.json

# Exclude any default go build binary
{{.repoName}}

# Exlcude test results in the fixtures path
fixtures/results/*.json
fixtures/results/*.sql
fixtures/results/*.txt

# Exclude default binaries from cmd in the bin dir
bin/server
bin/cli

# No more known_hosts
**/known_hosts
